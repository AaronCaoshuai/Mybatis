<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aaron.mybatisdemo.mapper.UserMapper">

    <select id="findUserById" parameterType="int" resultType="com.aaron.mybatisdemo.domain.User">
        select * from user where id = #{id};
    </select>

    <select id="findUserByName" parameterType="java.lang.String" resultType="com.aaron.mybatisdemo.domain.User">
        select * from user where username like '%${value}%'
    </select>

    <insert id="insertUser" parameterType="com.aaron.mybatisdemo.domain.User">
        <!--主键返回 主键属性 主键生成是在插入前,插入后(mysql插入后生成) 返回结果类型-->
        <selectKey keyProperty="id" order="AFTER" resultType="java.lang.Integer">
            select LAST_INSERT_ID() <!--mysql的主键返回函数-->
        </selectKey>
        insert into user(username,birthday,sex,address)
        values(#{username},#{birthday},#{sex},#{address})
    </insert>
    <delete id="deleteUserById" parameterType="int">
        delete from user where id = #{id}
    </delete>

</mapper>