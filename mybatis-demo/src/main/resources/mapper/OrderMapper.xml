<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aaron.mybatisdemo.mapper.OrderMapper">
    <!--一对一结果集-->
    <resultMap id="orderUserMap" type="com.aaron.mybatisdemo.domain.Order">
        <id column="id" property="id"/>
        <result column="remark" property="remark"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
        <result column="user_id" property="userId"/>
        <association property="user" select="getUser" column="user_id">
           <id column="id" property="id"/>
           <result column="username" property="username"/>
           <result column="birthday" property="birthday"/>
           <result column="address" property="address"/>
           <result column="sex" property="sex"/>
        </association>
    </resultMap>

    <select id="getUser" resultType="user">
        select * from user where id = #{user_id}
    </select>

    <select id="selectUserByOrderId" parameterType="int" resultMap="orderUserMap">
        select o.*
        from `order` o where o.id = #{orderId}
    </select>

</mapper>